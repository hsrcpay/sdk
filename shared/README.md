# Shared Assets

Cross-language source-of-truth artifacts.

- `openapi/` API schemas and versioning policy placeholders
- `contracts/` shared error model and pagination contract placeholders
- `test-vectors/` language-neutral test fixtures placeholders
